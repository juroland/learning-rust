# Learning Rust

This repository tracks my progress in learning Rust.

## Current Progress

### Fundamental Concepts Learned

- **Error Handling** - Using `?` operator to propagate `None`
- **Generic Functions** - Using type parameters with trait bounds
- **Iterators** - `.iter()`, `.map()`, `.sum()` for functional operations
- **Option Type** - Handling optional values with `Some` and `None`
- **Ownership & Borrowing**
- **Trait Bounds** - `Ord`, `Copy`,...

## Running Tests

```bash
cargo test
```
